name: credits-server
layer: feature_servers
desc: Server handling credits and virtual currency operations
github: "https://github.com/decentraland/credits-server"
owner: null
lib_deps: [schemas, core-components, platform-crypto-middleware]

calls:
  - Squid Indexer(HTTP (GraphQL)): Queries on-chain credit consumption data

called_by: []
