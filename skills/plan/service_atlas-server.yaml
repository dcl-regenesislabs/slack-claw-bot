name: atlas-server
layer: feature_servers
desc: Server providing map and atlas-related data
github: "https://github.com/decentraland/atlas-server"
owner: null
lib_deps: [schemas]

calls:
  - WorkerSubgraph(HTTP (GraphQL)): Queries blockchain data for map information

called_by: []
