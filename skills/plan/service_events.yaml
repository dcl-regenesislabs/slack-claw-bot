name: events
layer: feature_servers
desc: Events platform frontend (Gatsby-based)
github: "https://github.com/decentraland/events"
owner: null
lib_deps: [schemas]

calls:
  - catalyst(HTTP): Fetches event and scene data via catalyst-client

called_by: []
