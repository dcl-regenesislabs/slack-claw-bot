name: events-notifier
layer: feature_servers
desc: Event notification and scheduling service
github: "https://github.com/decentraland/events-notifier"
owner: null
lib_deps: [schemas, core-components]

calls:
  - notifications-workers(SNS/SQS): Publishes event notifications
  - badges(SNS/SQS): Publishes badge-related events

called_by:
  - notifications-workers(SNS/SQS): Consumes events for notification processing
